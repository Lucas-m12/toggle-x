version: '3.8'

services:
  postgres:
    image: postgres:15
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: togglex
      POSTGRES_PASSWORD: togglex
      POSTGRES_DB: togglex
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
